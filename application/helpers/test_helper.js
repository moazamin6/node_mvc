"use strict";

let helpers = {
   test_helper: () => {
      return 'This is helper from custom test helper';
   },
   test2_helper: () => {
      return 'This is helper from custom test helper';
   }
};

module.exports = helpers;